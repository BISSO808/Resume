<body>
  <div class="container">
  
        {{#each education}}
        <div class="row alert alert-primary">
        <div class="col-sm-10">
        {{this.degree}}
        {{this.institution}}
        {{this.end}}
        {{this.start}}
        </div>
        <div class="col-sm-2">
          <a class="btn btn-danger" align="right" href="education/delete/{{this.id}}">
          delete
          </a>
        </div>
         </div>
          {{/each}}
   </div>
      

</body>